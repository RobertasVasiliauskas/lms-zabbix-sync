[project]
name = "lms-zabbix-sync"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "geopy>=2.4.1",
    "pika>=1.3.2",
    "pyzabbix>=1.3.1",
]
